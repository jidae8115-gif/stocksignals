name: RecommendationsWatch

on:
  schedule:
    - cron: "*/5 * * * *"   # GitHub Actions 스케줄 최소 간격(5분) — 공개 저장소라 Actions 무료 시간 무제한
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: recommendations-watch
  cancel-in-progress: false

env:
  TZ: Asia/Seoul   # datetime.now() 기반 is_market_hours() 판정이 한국시간 기준이어야 함
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

jobs:
  watch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements-cloud.txt

      - name: Write service account key
        run: echo '${{ secrets.SERVICE_ACCOUNT_JSON_B64 }}' | base64 -d > service_account.json

      - name: Run watch_recommendations.py
        run: python watch_recommendations.py

      - name: Commit updated state
        run: |
          git config user.name "stocksignals-bot"
          git config user.email "actions@github.com"
          git add latest_recommendations.csv last_scan_recommendations.json notified_state.json recommendations_log.csv 2>/dev/null || true
          git diff --staged --quiet || git commit -m "chore: update recommendations [skip ci]"
          git pull --rebase
          git push
